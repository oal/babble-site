name: User
options:
    admin:
        icon: users
        list_display:
        - id
        - name
        - is_active

fields:
    name:
        name: Name
        type: text

    password:
        name: Password
        type: password

    is_active:
        name: Active
        type: boolean

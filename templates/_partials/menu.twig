{% spaceless %}
<nav>
    <ul>
        {% for page in Page.orderBy('title', 'desc') %}
            <li{% if path.isExactly(page.getAbsoluteURL()) %} class="active"{% endif %}>
                <a href="{{ page.getAbsoluteURL() }}">{{ page.title }}</a>
            </li>
        {% endfor %}
        <li {% if path.is('/docs') %} class="active"{% endif %}>
            <a href="/docs/">Documentation</a>
        </li>
    </ul>
</nav>
{% endspaceless %}
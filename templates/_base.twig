<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{% block title %}{% endblock %} - {{ site.title }}</title>
    <meta name="viewport" content="width=device-width,minimum-scale=1,initial-scale=1">
    <link rel="amphtml" href="{{ path }}.amp">
<link href="https://fonts.googleapis.com/css?family=Open+Sans:400,800" rel="stylesheet">
    <link rel="stylesheet" href="/static/main.css">
    {% block head %}{% endblock %}
</head>
<body>

<header id="header">
    <h2>
        <a href="/">
            <img src="/static/logo.svg" alt="Babble CMS">
        </a>
    </h2>
    <nav>
        <ul>
            {% for page in Page.orderBy('title', 'desc') %}
                <li>
                    <a href="/{{ page.id == 'index' ? '' : page.id }}">{{ page.title }}</a>
                </li>
            {% endfor %}
            <li><a href="/docs/">Documentation</a></li>
        </ul>
    </nav>
</header>
{# 
{{ path }} - 
{{ path.getExtension() }} -
{{ path.getBase() }} - #}

{% block body %}{% endblock %}

<footer id="footer">
    Powered by Babble CMS.
</footer>

{% block bottom %}{% endblock %}

</body>
</html>